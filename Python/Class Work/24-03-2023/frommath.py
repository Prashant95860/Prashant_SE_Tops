# through import math we all the time used math.factorial

# all function fetched in this

from math import *

print(factorial(5))

# with the help of function name we fetched only function that we want

from math import factorial,ceil

print(factorial(5))
print(ceil(56.85))
